// File intentionally emptied — App-specific error classes removed (issue #225).
// AppNotInstalledError and AppPrivateKeyError were only used by the deleted
// auth.ts and private-key.ts modules.
export {};
