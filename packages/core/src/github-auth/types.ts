// File intentionally emptied — App-specific types removed (issue #225).
// GitHubAppConfig, CachedInstallationToken, IGitHubAppAuthProvider were all
// GitHub App constructs. With OAuth App auth, the adapter needs no auth
// config object — it gets getUserToken from opts and that's it.
export {};
