import { describe, test } from 'bun:test';

describe('WorkflowNodeRetryAction', () => {
  test.todo('renders a retry action for an eligible failed node', () => {
    // Pending until WorkflowNodeRetryAction lands in Phase 5.2.
  });
  test.todo('opens a confirmation dialog before dispatching retry-node', () => {
    // Pending until WorkflowNodeRetryAction lands in Phase 5.2.
  });
  test.todo(
    'confirmation copy explains tracked reset, safety ref or commit, untracked preservation, and rerun scope',
    () => {
      // Pending until WorkflowNodeRetryAction lands in Phase 5.2.
    }
  );
  test.todo('shows a loading state and prevents duplicate retry submissions', () => {
    // Pending until WorkflowNodeRetryAction lands in Phase 5.2.
  });
  test.todo('surfaces retry API errors and keeps the action recoverable', () => {
    // Pending until WorkflowNodeRetryAction lands in Phase 5.2.
  });
});
