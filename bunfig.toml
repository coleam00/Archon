# Bun configuration for Remote Agentic Coding Platform

[test]
# Only discover tests in packages/ - prevents finding duplicates in workspace/worktrees
root = "./packages"
# Tests never send telemetry (scripts/test-telemetry-off.ts)
preload = ["./scripts/test-telemetry-off.ts"]
# Coverage output
coverage = true
coverageDir = "coverage"
