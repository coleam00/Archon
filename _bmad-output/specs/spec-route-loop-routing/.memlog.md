---
topic: Route loop routing for controlled workflow review and fix loops
updated: 2026-06-26T19:17
---

- (direction) User required every decision from plans/grill-me/260625-2337-route-loop-decisions.md to be preserved in the spec contract.
- (decision) Create a route loop routing spec with SPEC.md plus companions, with decision-catalog.md as the normative preservation surface for D001 through D109 and O005.
- (capability) CAP-1: Authors can declare route_loop as a standalone controller node with from, condition, max_iterations, and required positive negative exhausted routes.
- (capability) CAP-2: Runtime can execute bounded negative route loops, create fresh attempts for rerun path nodes, and exit through positive or exhausted outcomes.
- (capability) CAP-3: Route loop conditions reuse the existing when grammar while failing fast on parse errors and invalid output references instead of silently routing negative.
- (capability) CAP-4: Runtime exposes node_routed events, route_loop.output metadata, per-node attempts, execution sequence, and preserved attempt history for audit and debug.
- (capability) CAP-5: Resume and retry preserve route activation state, loop counters, and attempt counters, and retry-node continues through the router from the new source result.
- (capability) CAP-6: Web builder authors route_loop through one synchronized input edge and three output ports that serialize to route_loop.routes.
- (constraint) The depends_on graph remains acyclic and runtime cycles are allowed only through guarded route_loop negative paths.
- (constraint) Existing loop, depends_on, when, trigger_rule, lifecycle, and workflow migration behavior must remain backward compatible because route_loop is additive.
- (constraint) No arbitrary cyclic executor, node-level routes, routes.default, global execution cap, terminal route sentinel, multi-target route, route-loop prompt injection, or route_loop retry node target is in scope.
- (note) Persistent facts file project-context.md was configured but absent from the repository, so brownfield context is based on direct repo inspection and the source decision log.
- (note) Source bmad-target-follow.md defines the BMAD story flow SS CS VS TD AT DS TA CR RV NR TR then next story, with review failure returning to DS.
- (note) Brownfield inspection found current DAG nodes are mutually exclusive prompt command bash script loop approval cancel modes, loader validates acyclic depends_on, and executor runs topological layers concurrently.
- (event) Activation completed with no prepend or append hooks; customization, template, config, source decision log, target flow source, and relevant repo context were read.
- (event) Coherence validation passed: SPEC.md has six capabilities with intent and success, explicit constraints, explicit non-goals, concrete success signal, and lean companion split.
- (event) Preservation validation passed: decision-catalog.md contains all 109 Grill Me D decisions plus G001 and O005, companions absorb source context and brownfield repo findings, and wrapper-only process content was omitted.
- (note) Second codebase scout confirmed route_loop node_routed and loopCounters are absent today, existing router is conversation-to-workflow routing, existing loop is prompt-loop behavior, and brownfield.md was updated with implementation surfaces and caveats.
