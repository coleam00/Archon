# Exercise the concern child as a standalone workflow. The real verdict script
# checks the structured diagnosis coverage and the outputs of both independent
# phases; the external validation commands and GitHub delivery remain stubbed.
fixture:
  expect: completed
  inputs:
    order: >-
      {"concern_id":"cli-json-envelope","title":"Keep CLI JSON output machine-readable","branch":"fix/cli-json-envelope","kind":"split-or-recompose-test","diagnosis_ids":["cli-json-envelope"],"owned_paths":["packages/cli/src/commands/workflow.test.ts"],"shared_primitives":["runCli"],"why_grouped":"One concern owns the CLI JSON envelope assertions.","diagnoses":[{"diagnosis_id":"cli-json-envelope","confirmed":true,"title":"CLI JSON output shares one expensive setup","file":"packages/cli/src/commands/workflow.test.ts","test_id":"workflow JSON envelope","kind":"split-or-recompose-test","mechanism":"Each case repeats the same subprocess setup.","invariant":"Every JSON command emits exactly one parseable envelope.","evidence":"The cases share the same setup and assertion boundary.","owned_paths":["packages/cli/src/commands/workflow.test.ts"],"shared_primitives":["runCli"],"forbidden_residuals":["timeout increase","retry","skipped assertion"],"verification":"Run the focused workflow command test once."}]}
    smell: Repeated subprocess setup crosses the test deadline on Windows.
exec-code: true
implement:
  branch: fix/cli-json-envelope
  summary: Share setup while preserving one assertion per JSON command.
  validate_cmd: bun run validate
  test_cmd: bun test packages/cli/src/commands/workflow.test.ts
  addressed_diagnosis_ids: [cli-json-envelope]
verify:
  green: true
  targeted:
    command: bun test packages/cli/src/commands/workflow.test.ts
    passed: true
    artifact: /tmp/stabilizer-targeted.log
  project:
    command: bun run validate
    passed: true
    artifact: /tmp/stabilizer-project.log
review:
  approved: true
  summary: The concern is fixed and its invariant remains live.
  findings: []
ship:
  concern: Keep CLI JSON output machine-readable
  branch: fix/cli-json-envelope
  pr_url: https://example.com/pr/1
  ci_verdict: green
  ci_detail: validate=pass
