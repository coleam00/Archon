diagnose:
  status: inconclusive
  summary: 'The dry-run base cannot resolve to a checked revision.'
  findings: []
fixture:
  expect: completed
  # The deterministic nodes run for real here; only the agents are stubbed.
  reached: [prepare, collect, finish]
exec-code: true
