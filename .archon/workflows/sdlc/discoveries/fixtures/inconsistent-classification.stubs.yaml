# Agent outputs are simulated; script nodes execute through the fixture runner.
revalidate:
  entries:
    - item_index: 0
      verdict: supported
      evidence_refs: [{ path: "AGENTS.md", line: 999999 }]
      note: "stub: claims still valid, but the cited line is out of range"
search-existing:
  entries:
    - item_index: 0
      forge_checked: false
      matches: []
classify:
  entries:
    - item_index: 0
      classification: stale
      target_item: null
      public_title: "Example source finding"
      public_summary: "A fixture proposal for reviewer inspection."
      disclosure_safe: true
      rationale: "stub: deliberately labels missing evidence stale without a disproved verdict"
fixture:
  expect: failed
  fail-node: render
  reached: [resolve-input, revalidate, check-evidence, search-existing, classify]
  inputs:
    discovery_artifact: ".archon/workflows/sdlc/discoveries/fixtures/data/single.json"
exec-code: true
