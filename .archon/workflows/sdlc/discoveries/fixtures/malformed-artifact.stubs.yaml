# Agent outputs are simulated; script nodes execute through the fixture runner.
fixture:
  expect: failed
  fail-node: resolve-input
  inputs:
    discovery_artifact: ".archon/workflows/sdlc/discoveries/fixtures/data/malformed.json"
exec-code: true
