# Guard fixture: discovery_artifact exists but is not valid JSON.
# resolve-input (real script) must fail cleanly rather than proceed on
# nothing. Nothing downstream ever runs, so no other node needs a stub.
fixture:
  expect: failed
  fail-node: resolve-input
  inputs:
    discovery_artifact: ".archon/workflows/sdlc/discoveries/fixtures/data/malformed.json"
exec-code: true
