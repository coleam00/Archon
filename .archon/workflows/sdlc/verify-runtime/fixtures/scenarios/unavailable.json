{
  "assertions": [
    {
      "id": "unreachable-check",
      "description": "A check that can never run because the declared start command always fails"
    }
  ],
  "environment": {
    "start": "exit 1",
    "candidate_command": "echo fixture-candidate-001"
  }
}
