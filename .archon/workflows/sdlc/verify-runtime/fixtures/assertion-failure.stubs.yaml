# A real, evidence-backed failed assertion. Every structural check still
# passes (coverage, evidence, candidate) — this is what distinguishes
# `failed` from `malformed`: the report is entirely trustworthy, and the
# candidate genuinely did not do what was declared. Never retried.
verify:
  candidate: "fixture-candidate-001"
  assertions:
    - id: help-text
      ok: true
      observed: "python3 --version printed 'Python 3.11.6'"
      evidence_path: .archon/workflows/sdlc/verify-runtime/fixtures/evidence/help-text.txt
    - id: always-true
      ok: false
      observed: "the check unexpectedly exited 1 instead of 0"
      evidence_path: .archon/workflows/sdlc/verify-runtime/fixtures/evidence/always-true.txt
exec-code: true
fixture:
  expect: completed
  reached: [gate-verified]
  inputs:
    scenario: .archon/workflows/sdlc/verify-runtime/fixtures/scenarios/sample.json
