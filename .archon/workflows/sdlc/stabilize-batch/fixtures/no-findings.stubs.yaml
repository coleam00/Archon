# Fixture (acceptance test for #2818 — red until dry-run simulates workflow: nodes):
# sniff finds nothing — the run completes honestly with zero orders.
sniff:
  grounded: "no reachable CI evidence; sweep of test files found no matching pattern"
  findings: []
work-orders:
  orders: []
fixture:
  expect: completed
exec-code: false
