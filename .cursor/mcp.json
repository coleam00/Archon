{
  "mcpServers": {
    "scout-apm": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "--env", "SCOUT_API_KEY", "scoutapp/scout-mcp-local:latest"],
      "env": {
        "SCOUT_API_KEY": "${env:SCOUT_API_KEY}"
      },
      "envFile": "${workspaceFolder}/.env"
    }
  }
}
